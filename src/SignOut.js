import { View, Text, Button } from "react-native";

const SignOut = () =>{
    return(
        <View style={{marginTop:200, marginRight:300, marginLeft:20}}>
            <Button title= "Sign Out"/>
        </View>
    )
}
export default SignOut;